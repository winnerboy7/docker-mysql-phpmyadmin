services:
  # db:
  #   image: mysql:8.0
  #   container_name: mysql-server   
  #   restart: unless-stopped
  #   tty: true
  #   environment:
  #     TZ: Asia/Bangkok
  #     MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD} # Replace with a strong password
  #     MYSQL_DATABASE: ${MYSQL_DATABASE}           # Optional: Create a default database
  #     MYSQL_USER: ${MYSQL_USER}                   # Optional: Create a default user
  #     MYSQL_PASSWORD: ${MYSQL_PASSWORD}           # Optional: Set password for the default user
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - ./mysql/data:/var/lib/mysql/
  #     - ./mysql/config:/etc/mysql/conf.d/
  #     - ./mysql/initdb:/docker-entrypoint-initdb.d
  #   healthcheck:
  #     test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p$$MYSQL_ROOT_PASSWORD"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - app-network

  phpmyadmin:
    image: phpmyadmin:latest
    # image: phpmyadmin/phpmyadmin:latest
    container_name: mysql-cluster-admin
    restart: always
    ports:
      - 8080:80
    environment:
      TZ: Asia/Bangkok
      UPLOAD_LIMIT: 1024M
      MEMORY_LIMIT: 1024M
      PMA_HOST: ${MYSQL_HOST}
      PMA_PORT: ${MYSQL_PORT}  
      PMA_ABSOLUTE_URI: "https://training.scoutthailand.org/admindb/"
      # PMA_ARBITRARY: "0"
      # MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      # PMA_ARBITRARY: 1
    # depends_on:
    #   - db
    networks:
      - app-network

networks:
  app-network:
    name: app-network
    driver: bridge